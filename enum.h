#ifndef ENUM_H
#define ENUM_H

/* ---------------------------------------------------------------------[<]-
 Enum: states
 Synopsis: Desсribes the type of the cell.
 ---------------------------------------------------------------------[>]-*/
enum states{
    NONE,
    TARGET,
    CONDITION
};

#endif